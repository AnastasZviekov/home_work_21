 /* let figure

           function count() {
                  indexArr.push(index)
               return indexArr
           }

           count()


           const duplicates = indexArr.filter((e, ind, arr) => {
              if(arr.indexOf(e) !== ind) {arr.splice(arr.indexOf(e),1)}  }   )

           console.log(indexArr)*/



/*
            this.setState( {
                newColor: this.changeColor(),
                newColor: this.chooseObj(), },*/



 function shuffle (array) {
            let i = 0, j = 0, temp = null
            for (i = array.length - 1; i > 0; i -= 1) {
                j = Math.floor( Math.random() * (i + 1) )
                temp = array[i]
                array[i] = array[j], array[j] = temp
            }
        }

    */



*
     chooseObj (index) {

        this.state.list.filter( (el, i) => {
            if ( i===index) {
                el.active = true
            };
        } )

    }*/



/*export default function List ({list=[]}){
    return list.map((item,index)=>  <tr key={index}> <td>{item.type}</td> <td>{item.icon}</td></tr>)
}*/